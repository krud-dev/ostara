import { Theme } from '@mui/material/styles';
import { Components } from '@mui/material/styles/components';

export default function Grid(theme: Theme): Components {
  return {
    MuiGrid: {
      styleOverrides: {},
    },
  };
}
